using UnityEngine;
using UnityEngine.UIElements;

public class RecipesPage : BasePage
{
    protected override void SetupPage()
    {
        
    }

    public RecipesPage(VisualElement root, Header header, string name) : base(root, header, name)
    {
    }
}
