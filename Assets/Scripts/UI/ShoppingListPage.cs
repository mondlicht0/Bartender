using UnityEngine;
using UnityEngine.UIElements;

public class ShoppingListPage : BasePage
{
    protected override void SetupPage()
    {
        
    }

    public ShoppingListPage(VisualElement root, Header header, string name) : base(root, header, name)
    {
    }
}
