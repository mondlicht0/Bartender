using UnityEngine;
using UnityEngine.UIElements;

public class LessonsPage : BasePage
{
    protected override void SetupPage()
    {
        
    }

    public LessonsPage(VisualElement root, Header header, string name) : base(root, header, name)
    {
    }
}
